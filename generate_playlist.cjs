const fs = require('fs'); const path = require('path'); const publicDir = path.join(__dirname, 'public', 'songs'); const batch2Dir = path.join(publicDir, 'batch2'); const batch3Dir = path.join(publicDir, 'batch3'); let playlist = []; if (fs.existsSync(publicDir)) { const files = fs.readdirSync(publicDir); files.forEach(f => { if (f.endsWith('.webm') || f.endsWith('.mp3') || f.endsWith('.m4a')) { playlist.push('/songs/' + f); } }); } if (fs.existsSync(batch2Dir)) { const files = fs.readdirSync(batch2Dir); files.forEach(f => { if (f.endsWith('.webm') || f.endsWith('.mp3') || f.endsWith('.m4a')) { playlist.push('/songs/batch2/' + f); } }); } if (fs.existsSync(batch3Dir)) { const files = fs.readdirSync(batch3Dir); files.forEach(f => { if (f.endsWith('.webm') || f.endsWith('.mp3') || f.endsWith('.m4a')) { playlist.push('/songs/batch3/' + f); } }); } fs.writeFileSync(path.join(__dirname, 'src', 'playlist.json'), JSON.stringify(playlist, null, 2)); console.log('Playlist generated with ' + playlist.length + ' songs');
